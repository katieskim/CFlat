int main()
{
    note n;
    note m;
    tone a;

    tone t;
    tone v;

    n = (/C/ /4/ /s./);
    m = (/G/ /5/ /q/);

    t = n.tone();       (: new get function, it should return /C/ :)
    printt(t);          (: /C/ :)

    n.tone(/F/);        (: new set function, it should set tone of n to /F/ :)
    printt(n.tone());   (: /F/ :)
    printt(t);          (: /C/ :)

    v = m.tone();
    printt(v);          (: /G/ :)

    (: printt(m.raiseTone(1));   raises tone of note m by half step, returns new note :)
    (: printt(a);     /G+/ or /A-/ :)

	return 0;
}