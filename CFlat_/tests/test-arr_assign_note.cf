int main()
{
  note[] note_arr;
  note n;

	n = ( /C-/ /4/ /q/ );
  note_arr = (note[]) [n,n,n,n];  
  print(note_arr[1]);               
  (: should print out the array literal, the output file for this test is probably wrong :) 
  return 0;
}
