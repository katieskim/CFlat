
struct note_arr {
    struct note *data;
    int size;
};


/*allocate space for a single note arr*/
/*Create enough space for however long the array is actually is = size of struct note * number of notes */ 
/*set data (the notes) to the array that i allocated */
/*set the size field  = the number of notes */
/* fill in each element (note) of the data array */


/*inialize note give pointer to a note,  */

